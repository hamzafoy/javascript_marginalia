/*-------------------------------------
~~~| Miscellaneous Notes on HTML5/CSS3 |~~~
---------------------------------------- */

/*--
1. background-image {

}
will not allow screen-readers to see `alt` properties. use <img/> tags instead for accessibility.
--*/

/*--
2. HTML naming convention should make plain & apparent what are parent elements & their children.

EX:
<div class="card">Parent Element
    <div class="card__title">
    Child Element
    </div>
</div>
--*/

/*--
3. Use `min-height` to set height constraints instead of `height` to avoid overflow.
--*/